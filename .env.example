# Copy to .env and fill in. .env is gitignored — never commit the token.
FOOTBALL_DATA_TOKEN=your-football-data-org-token
COMPETITION=WC
POLL_INTERVAL=120
# Optional: run a command once per NEW finished match (notification, etc.)
# ON_RESULT_CMD=echo "$HOME $HOME_SCORE-$AWAY_SCORE $AWAY"
